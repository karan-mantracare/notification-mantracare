export default function SMSSettingsPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1 style={{ fontSize: "1.5rem", fontWeight: "600", color: "var(--dark)", marginBottom: "1rem" }}>SMS Settings</h1>
      <div className="card" style={{ padding: "2rem", textAlign: "center", color: "var(--text-muted)" }}>
        <p>SMS settings configuration coming soon.</p>
      </div>
    </div>
  );
}
